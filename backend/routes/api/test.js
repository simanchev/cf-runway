// const test = require('express').Router();
const finData = [{
  title: 'Расходы', type: '', date: 'октябрь', regular: true, sum: 150, id: 1,
}];
const fin_data = [{
  finTypesId, projectId, title, sum, date, regular,
}];
const schedule = [{
  findataID, m1, m2, m3, m4, m5, m6, m7, m8, m9, m10, m11, m12,
}];
function count() {
 
}

// test.get('/', (req, res)=>

// )

// module.exports = test
